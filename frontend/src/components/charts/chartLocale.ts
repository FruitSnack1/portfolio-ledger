import { APP_INTL_LOCALE } from '../../intl/appLocale'

/** Re-export for lightweight-charts `localization.locale` (English month names on the time axis). */
export const CHART_INTL_LOCALE = APP_INTL_LOCALE
