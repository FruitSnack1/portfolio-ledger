DATABASE_URL=postgresql://postgres:postgres@127.0.0.1:5432/portfolio_ledger
JWT_SECRET=replace-with-at-least-32-random-characters

# When the UI is on another origin (e.g. Vercel) and calls this API directly, set your frontend URL(s):
# CORS_ORIGINS=https://your-app.vercel.app
# Cross-site session cookie (requires HTTPS on the API). Omit both for local same-origin dev only.
# AUTH_COOKIE_SAME_SITE=none
